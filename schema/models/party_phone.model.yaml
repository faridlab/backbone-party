# =============================================================================
# Domain: Party
# Entity: PartyPhone
# Description: One of a party's phone numbers (multi-value). Adopted from salt-laravel-contacts.
# =============================================================================

models:
  - name: PartyPhone
    collection: party_phones
    description: "A phone number of a party"

    fields:
      id:
        type: uuid
        attributes: ["@id", "@default(uuid)"]
        description: "Unique phone identifier"

      party_id:
        type: uuid
        attributes: ["@required", "@foreign_key(Party.id)"]
        description: "Owning party"

      # Tenant scope (ADR-0010 B1) — logical FK to organization.Company.id (NO hard SQL FK).
      # Denormalized from Party for per-company RLS fencing on this table.
      company_id:
        type: uuid
        attributes: ["@required", "@exclude_from_foreign_key_check"]
        description: "Tenant scope # logical FK to organization.Company.id"

      label:
        type: string
        attributes: ["@default('mobile')", "@max(30)"]
        description: "Label (mobile / work / home / fax / other)"

      phone:
        type: string
        attributes: ["@required", "@max(30)"]
        description: "Phone number"

      is_primary:
        type: bool
        attributes: ["@default(false)"]
        description: "Primary phone for the party"

      metadata:
        type: Metadata
        attributes: ["@audit_metadata"]
        description: "Audit metadata (created_at, updated_at, deleted_at, created_by, updated_by, deleted_by)"

    relations:
      party:
        type: Party
        attributes: ["@one", "@foreign_key(party_id)"]
        description: "Owning party"
        inverse: phones

    indexes:
      - type: index
        fields: [company_id, party_id, is_primary]
      # At most ONE primary per party (council 2026-07-02) — soft-delete aware, per-company (ADR-0010 B1).
      - type: unique
        fields: [company_id, party_id]
        where: "is_primary AND deleted_at IS NULL"
